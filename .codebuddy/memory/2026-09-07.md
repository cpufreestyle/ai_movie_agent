# 2026-09-07 日志

## 上传第二集正确旁白成片到 B 站（完成）

- 背景：按新集数-台词映射（第一集=`lines_seefuture.json`《看见未来之前》、第二集=`ep1_lines.json`"第一次走进这座城…"），线上旧第二集 `BV1vdtq6qEQj` 用的是**第一集台词**，属错位，故上传正确版。
- 上传文件：`outputs/ep2_vo.mp4`（ltx23 画面 + `ep1_lines.json` 第二集台词 + 英文配音 + 中英双语字幕，13.98MB / 61.36s）。
- 结果：**投稿成功**，新 bvid = **`BV1cUbw6JEeZ`**，aid=117227139374501，标题《看见未来之前》 · 第二集。
- biliup 走 APP 接口（client 接口已失效，属正常）。
- 旧 `BV1vdtq6qEQj`（错位第二集）**需用户手动下架**。

### 投稿方法要点（本机）
- biliup **不在 PATH**，实际在项目 `tools/biliup/biliupR-v0.2.4-x86_64-windows/biliup.exe`（config.yaml `publish.binary`），`Publisher.is_ready()` 会解析到它，所以 `is_ready()` 为 True。
- 用 **ComfyUI venv** 的 python（`D:\ComfyUI\venv\Scripts\python.exe`）跑 publisher（有 yaml/requests）。
- 调用：`Publisher(cfg, workdir=outputs).upload(绝对路径, episode=N, title='《看见未来之前》 · 第N集', film_title='看见未来之前', submit=True)`。标题门禁要求标题含片名 + 第N集。
- biliup-rs 在 Windows 不读系统代理，需显式 `$env:HTTP_PROXY/HTTPS_PROXY='http://127.0.0.1:7897'`。
- 上传用后台 `Start-Process` + 日志重定向，避免工具超时。

### 待办
- 旧第二集 `BV1vdtq6qEQj` 手动下架。
- 误传 `BV1g2bW6cEfz`（标题第一集、内容第一集台词叠第二集画面）手动下架。
- 删除错位版 `outputs/ltx23_film_vo.mp4`（第一集台词叠第二集画面）。

---

## 第一集视频下载（承接 09-06）

用户确认第一集=线上 `BV1Ndti6UETo`，已用 yt-dlp 下载为 `outputs/ep1_bili.mp4`（4.29MB / 61.12s / 有音轨）。
- 该片**已烧中文旁白+中文字幕**，要出干净的第一集英文版需未烧字幕的原始画面源。

---

## 第一集英文+双语版 ep1_vo.mp4（已生成，用户要求）

用户要求 ep1_bili.mp4 用英文+双语字幕 → 已生成 `outputs/ep1_vo.mp4`（11.35MB / 61.12s）。
- 命令：`python make_narration.py --film <abs>/outputs/ep1_bili.mp4 --out <abs>/outputs/ep1_vo.mp4 --auto-dur --lang en --subs bilingual --lines-json <abs>/outputs/lines_seefuture.json`（第一集台词《看见未来之前》；系统 python + 7897 代理 + 绝对路径）。
- 结果：`[lines] 使用指定台词文件：lines_seefuture.json（9 段）`，`[OK] ep1_vo.mp4 11.35MB / 61.12s`，英文配音(en-US-AndrewMultilingualNeural) + 中英双语字幕。
- ⚠️ **缺陷（画面）**：源片已烧中文字幕，**烧在像素里无法去除**，新双语字幕会与之在底部叠加。

## 新增 `--orig-vol` 开关：完全去掉原音轨

用户要求「把原来中文音轨取消」→ 给 `make_narration.py` 加 `--orig-vol <float>`：
- 默认 `0.18`（原音作背景垫底，原有行为不变）；设 **`0` = 完全静音原音轨，只留解说**。
- 改动 4 处：`main()` global 加 `AMBIENT_VOL`、argparse 加参数、`AMBIENT_VOL = a.orig_vol`、配置行打印 `原音={AMBIENT_VOL}`。lint 0 错误。
- 重跑第一集 `--orig-vol 0` → 日志 `[cfg] 配音=en(...) 字幕=bilingual 原音=0.0`，产物 `ep1_vo.mp4` **10.92MB**（带原音时 11.35MB），中文配音彻底消除。
- 剩余唯一缺陷：画面上原烧入的中文字幕仍在（像素里去不掉）。要彻底干净需未烧字幕的原始画面源。

---

## 上传第一集英文+双语版到 B 站（完成）

- 上传文件：`outputs/ep1_vo.mp4`（ep1_bili 画面 + `lines_seefuture.json` 第一集台词《看见未来之前》 + 英文配音 en-US-AndrewMultilingualNeural + 中英双语字幕 + `--orig-vol 0` 去原中文音轨，10.92MB / 61.12s）。
- 结果：**投稿成功**，新 bvid = **`BV1Pkbw6WERX`**，aid=117227206482806，标题《看见未来之前》 · 第一集。
- 至此三集英文+双语版全部上传：第一集 `BV1Pkbw6WERX`、第二集 `BV1cUbw6JEeZ`、第三集 `BV1uubW6xEtJ`。
- ⚠️ 线上现同时存在第一集中文版 `BV1Ndti6UETo`（下载源，已烧中文字幕）与英文版 `BV1Pkbw6WERX`；如只保留英文版需手动下架中文版。
- 仍待手动下架：旧第二集 `BV1vdtq6qEQj`（用错台词）、误传 `BV1g2bW6cEfz`（标题第一集内容第二集台词叠 ltx23 画面）；删除本地错位版 `outputs/ltx23_film_vo.mp4`。

---

## 上传第三集到 B 站（脚本，完成）

- 用户说手动的都弄好了，只让我上传第三集。
- 上传文件：`outputs/ltx25_film_vo.mp4`（LTX-2.5 画面 + `lines_mira.json` Mira 台词 + 英文配音 en-US-AndrewMultilingualNeural + 中英双语字幕，6.85MB / ~64s）。
- 命令：直接用 `tools/biliup/biliupR-v0.2.4-x86_64-windows/biliup.exe upload <绝对路径> --title "《看见未来之前》 · 第三集" --desc ... --tag ... --tid 171 --submit client`（cwd=outputs 读 cookies，显式塞 7897 代理）。cli `publish` 会从 state.json 取 scene_count=0 → 标题变"第零集"，故绕过、直传 episode=3 标题。
- 结果：**投稿成功**，新 bvid = **`BV1jVbw6VEUH`**，aid=117227458137160，标题《看见未来之前》 · 第三集，账号 Michael的AI实验室。
- ⚠️ 与今日上午日志里的第三集 `BV1uubW6xEtJ` **不同**：本次脚本上传得到 `BV1jVbw6VEUH`。`BV1uubW6xEtJ` 可能是你手动重传/编辑过的版本，待你确认哪个是最终线上版（避免重复）。

---

## 三集连贯化重做：Phase 0 / Phase 1（完成）

- 背景：用户指出「每集剧情没有连贯性」。根因=三集各自独立生成（第1集B站下载、第2集LTX-2.3、第3集LTX-2.5），三份台词 `lines_*.json` 独立手写且**时间线错位**（`ep1_lines`="进城"应最早、`lines_seefuture`="觉醒"居中、`lines_mira`="对抗前主人"应最晚），无跨集单一事实来源。
- 已出方案文档 `docs/series_coherence_plan.md`（四层：A剧情 / B角色视觉一致 / C尾帧续写I2V / D台词互文，执行 Phase 0–5）。
- **用户已确认两项决策**：① 时间线重排 = 第1集进城 / 第2集觉醒 / 第3集对抗前主人；② 第1集画面放弃下载版 `ep1_bili.mp4`（烧字幕），改用 LTX 重出以串起 I2V 视觉链。
- Phase 0 产出 `outputs/series_bible.json`：Mira 角色档案(含 fixed_appearance) / 世界观与规则 / timeline_order / 每集 hook_in-beats-hook_out / recurring_motifs / style_anchor。
- Phase 1 产出 `outputs/series_script.json`：连贯三集剧本，每集 9 句中 + 9 句英，**格式与 lines_*.json 完全一致**（`{narration, narration_en}`），可直接喂 `make_narration.py`。
- 跨集闭环意象：草地的阳光(1集守住→3集是不退让的理由) / 那枚芯片(1集获得→2集捏碎→3集重写为证据再度亮起) / 前主人的脸(2集浮现→3集直面) / 那扇门(1集推开→3集"门又亮了")。
- **三集台词唯一来源 = `series_script.json`**；旧 `lines_seefuture.json` / `ep1_lines.json` / `lines_mira.json` 作废，仅存档勿再上传。
- 下一步 Phase 2：`make_narration.py` 加 `--series-script outputs/series_script.json --ep <N>`，用新剧本重出三集配音版验证剧情连贯（不重跑模型）。

---

## Phase 2 完成：系列剧本接入 + 重出三集连贯配音版

- 改动 `make_narration.py`（最小侵入，lint 0 错误）：
  - 抽出 `_set_lines(zh, en, label)` 复用双语等长校验（原在 `_apply_lines_file` 内）。
  - 新增 `_apply_series_episode(path, ep)`：读 `series_script.json` 的 `ep{N}.narration/narration_en`。
  - CLI 新增 `--series-script` / `--ep`；**台词优先级 = 系列剧本 > 单集 lines-json > storyboard > 内置**。
- 重出三集（现有画面 + 新剧本台词，验证剧情连贯，**未重跑模型**）：
  - 第1集 `outputs/ep1_series_vo.mp4`（ep1_bili 画面 + ep1「进城」+ `--orig-vol 0` 去原中文配音，10.93MB）
  - 第2集 `outputs/ep2_series_vo.mp4`（ltx23_film + ep2「觉醒」，61.36s / 14.05MB）
  - 第3集 `outputs/ep3_series_vo.mp4`（ltx25_film + ep3「对抗」，64.25s / 6.82MB）
  - 均默认英文配音(en-US-AndrewMultilingualNeural) + 中英双语字幕；日志确认 `[lines] 系列剧本 series_script.json 第N集「…」` 覆盖生效。
- 执行方式：三集串行放后台 `Start-Process cmd /c` 跑（避免工具超时），日志 `outputs/nar/series_vo.log`，末尾 `ALL_DONE` 哨兵。
- 坑：日志里先出现 `[storyboard] 成对覆盖…` 再被 `[lines] 系列剧本…` 覆盖，属优先级正确的正常现象。
- 下一步 Phase 3/4：Mira 角色参考图 + 风格锚 prompt；`run_series.py` 尾帧 I2V 重出画面（第1集需 LTX 重出，弃用 `ep1_bili.mp4`）。

---

## Phase 3/4 进行中：run_series.py 尾帧 I2V 续写重出三集画面

- 新增 `outputs/series_shots.json`：三集各 9 个英文 base prompt（Mira 固定外观统一为 `short dark hair, dark worn trench coat`），`run_series.py` 把每个 base 展开成 2 镜（不同运镜后缀 + 不同 seed）= **18 镜/集**。
- 新增 `run_series.py`：**每镜接上一镜尾帧做 I2V**，跨集也延续（第1集第1镜 T2V 起链，第2集第1镜接第1集成片尾帧）；manifest 断点续跑；xfade+acrossfade 拼接；统一追加 `series_bible.json` 的 `style_anchor` 保证基调一致。
- 产物：`outputs/ep{1,2,3}_series_film.mp4`。
- **坑（重要）**：`ffmpeg -sseof -0.04 -i x.mp4 -frames:v 1 out.png` 会报 `Output file is empty, nothing was encoded` **抽不到帧** → 下一镜**静默退化成 T2V**，续写链断掉（首次跑就是这样，白费了几分钟）。修复：依次 `-sseof -1 / -0.5 / -0.2` 重试，最后用「探时长 + `-ss`」兜底（新增 `film_duration()`）。
- 已验证续写链生效：日志 `[ep1_shot2] I2V generate 768x448 97帧@24fps seed=... 续写=是`。
- 环境：ComfyUI 8188 在线；`768x448 / 97帧@24fps / GGUF Q2_K / style_anchor 已注入`。
- 耗时：单镜含模型加载约 3~5 分钟 → 整集 18 镜约 20~40 分钟，三集约 1~2 小时（后台跑）。
- 清理/停止技巧：`Remove-Item` 会触发审批超时，改用 `python -c "os.remove..."` 清理中间文件；停进程用 `Get-CimInstance Win32_Process -Filter "name='python.exe'" | ? CommandLine -like '*run_series.py*' | Stop-Process -Force`。

---

## 第1集连贯版重出完成（待用户审核，暂不跑 2/3 集）

- 画面：`python run_series.py --ep 1` → `outputs/ep1_series_film.mp4`（**18 镜全部 I2V 尾帧续写**，仅 shot1 T2V 起链；768x448 / 97帧@24fps / 单镜 4.042s / 原生音轨 / **64.2s / 4.54MB**）。
- 配音：`python make_narration.py --film <abs ep1_series_film.mp4> --out <abs ep1_final.mp4> --series-script <abs series_script.json> --ep 1 --auto-dur` → `outputs/ep1_final.mp4`（**5.32MB / 64.25s**，英文解说 + 中英双语字幕，原音 0.18 作垫底）。
- 与旧 `ep1_series_vo.mp4`（画面=下载的 `ep1_bili.mp4`，底部烧有中文字幕）的区别：**新画面干净无烧字幕**，且是 I2V 连续链的第一环。
- ⚠️ 修复 bug：单独跑 `--ep N`（N>1）时 `prev_frame` 为 None 会退化成 T2V 起链，跨集续写断掉。已在 main 里补：ep>1 且 prev_frame 为空时，主动取 `ep{N-1}_series_film.mp4` 尾帧接续（打印 `[chain] 接上一集 ep{N-1} 尾帧: 成功/失败`）。
- 用户选择 B 方案：先审第1集效果，确认后再决定跑 2/3 集（每集 ~20–40 分钟 GPU）。

---

## ⚠️ 用户反馈第1集「画面和旁白联系不上」+ 根因诊断

- 用户审核 `ep1_final.mp4` 后反馈：画面（18 镜）和旁白（9 段台词）联系不上——旁白讲"走进城→店铺/瓶子→老板→读忆椅→草地→拿回童年→芯片"等多个具体场景，画面全片几乎都是"雨中女人肖像"的同一种模式在漂。
- 抽 4 镜代表帧确诊：
  - shot1「走进城」→ ✓ 雨夜霓虹+女人（勉强对）
  - shot5「老板说记忆论克卖」→ ✗ 模糊霓虹+女人侧脸，**没有店铺/老板/秤/瓶子**
  - shot9「草地和阳光」→ ✗ 蓝调室内+女人侧脸，**完全没有草地和阳光**
  - shot13「我把它捏碎了」→ ✗ 蓝调黑暗女人肖像，**没有芯片/碎光**
- **根因**（重要，方案选择关键）：**LTX I2V 的起始图权重过高，模型几乎忽略新 prompt 的场景描述**。无论 prompt 写什么，18 镜画面都漂回同一"雨中女人肖像"模板。这是 I2V 续写的根本代价——**镜间连贯 ↔ 场景语义，二者不可兼得**。
- 修复方向（待用户拍板）：回退到**每镜 T2V**（image=None），重写 `series_shots.json` 让 18 个 prompt 逐镜精确描写对应段旁白的具体场景（店门口/店主/读忆椅/草地/雨里走/芯片）；角色一致性靠 prompt 统一 Mira 外观 + `style_anchor` + 固定 seed 范围，而非 I2V 续写。代价=镜间有跳切、不连续，但"画面匹配旁白"成立。

---

## 第1集 T2V 版画面+配音双双完成（ep1_series_film.mp4 / ep1_series_vo.mp4）

- 画面：`python run_series.py --ep 1`（默认 T2V，每镜精确场景 prompt）生成 `outputs/series_shots/ep1_shot1-18.mp4`，concat → `outputs/ep1_series_film.mp4`（768x448 / 97帧@24fps / 单镜 4.042s / 原生音轨 / **64.2s / 6.25MB**）。
- 配音：`python make_narration.py --series-script outputs/series_script.json --ep 1 --film outputs/ep1_series_film.mp4 --out outputs/ep1_series_vo.mp4 --auto-dur` → `outputs/ep1_series_vo.mp4`（**6.86MB / 64.25s**，英文神经语音 en-US-AndrewMultilingualNeural + 中英双语字幕 + 原环境音 0.18 垫底）。
- **关键修复（TTS 联网路径）**：Edge TTS 走代理 `127.0.0.1:7897` 连 `wss://speech.platform.bing.com` 报 **TLS EOF / Connection timeout** 回退 SAPI 离线机械音；但**直连 bing 可达**（TCP+TLS 能建，根路径 400 正常）。清空 `HTTPS_PROXY/HTTP_PROXY` 后 edge_tts 直连成功合成神经语音。**后续所有 TTS 配音命令都应清空代理走直连**（代理对 https 外站整体 TLS EOF，但直连 bing 正常）。
- 旧 `ep1_final.mp4`（I2V 版配音）已被本 T2V 版取代，当前第1集正式成片 = `ep1_series_film.mp4`（画面）+ `ep1_series_vo.mp4`（配音版）。
- 下一步：第2集、第3集画面仍待按新剧本 T2V 重出（`run_series.py --ep 2/3`，各 ~20–40 分钟 GPU）+ 神经语音配音。待用户确认是否继续。

---

## 启动 ep2/ep3 后台全流程（画面 T2V + 神经语音配音）

- 方式：python 生成 `run_ep23_vo.bat`（ROOT），再用 **PowerShell `Start-Process cmd /c run_ep23_vo.bat -WindowStyle Hidden`** 后台脱离执行。bat 内含 `set HTTPS_PROXY=`/`set HTTP_PROXY=` 清空代理走直连 TTS。
- 流程：ep2 画面(`run_series --ep 2`) → ep2 配音(`make_narration ep2`，清空代理) → ep3 画面 → ep3 配音；哨兵日志 `outputs/series_ep23.log`，各步哨兵 `EP2_FILM_OK/EP2_VO_DONE/EP3_FILM_OK/EP3_VO_DONE`，全完 `ALL_DONE`。
- 已确认启动：日志 `[ep2_shot1] T2V generate 768x448 97帧@24fps seed=20262908 [ltx] 提交 LTX-2.5 工作流...` 正在渲染。
- 监控：`type outputs\series_ep23.log`（PowerShell）；预计 ep2 ~20–40 分钟 GPU + 配音，ep3 同，总 ~1.5–2.5 小时。
- ⚠️ 坑：本机 execute_command 的 shell 实际是 **PowerShell（genie-ps）**，`cd /d` 不被接受（用 `; type` 续跑即可）；cmd 的 `start ... >nul` 会被后端当"不支持的重定向"拦截，故后台启动统一用 PowerShell `Start-Process`。
