# 2026-09-10

## 白模版人物一致性实验(ref_images 救回方案)
- 给 `agent/mmh3_engine.py` 的 `generate()` 增加 `ref_images` 参数:Autogrow 动态节点 `ref_images.ref_image_i`(最多9张),与 image/ref_video 相加组合(全部存在→Hybrid,有 ref_images 无图→Ref2VA)。
- `run_blocking.py` 加 `--ref-images`(逗号分隔,默认 mira_front/side/anchor 三张)+ `--no-turbo`。
- `_fix4.py` 验证:每镜 first_frame=锚定图 + ref_images=[mira_front, mira_side, mira_anchor],Turbo(4v/8a)。shot5 生成成功(1454KB,实际走 Hybrid+3参考图)。
- arcface 诊断(shot5,基准 mira_front):原白模脸检出 0% → 补身份 7.8% → 锚定换脸 17%(远景上限)。ref_images 有改善,但 shot5 是店铺外景,人物占比小、天花板低,不是最佳验证样本。
- 后台重跑 shot4/6/8/18 补身份版,待 arcface 对比(重点 shot4 人脸特写)判断救回效果。若仍低→关 Turbo 提步数再验。

## 部署控制台
- `launch_comfy.py` 启动 ComfyUI(`D:/ComfyUI/venv`, 127.0.0.1:8188, pid 70760, READY)。
- `webui.py` 启动项目 Web 控制台(127.0.0.1:8000, Flask 四页签:概览/运行监控/创意/发布)。
- 注意:之前用户把 `_fix4.py 4,6,8,18` 放后台,但当时 8188 关闭已废掉;ComfyUI 起后已重跑。

## 最终判定：白模"运镜增强版"路线死刑（arcface 数据坐实）
- 验证方式：`_fix4.py` 给每镜补 ref_images=[mira_front, mira_side, mira_anchor]，用 arcface(基准 mira_front) 对比 原白模 / 补身份 / 锚定换脸 三版。
- shot4(人脸特写，最干净样本)：原白模 0.308 → 补 ref_images 0.331(几乎无救) → 锚定+换脸 0.850(真救回)。
- 结论：白模 Hybrid 即使喂满 3 张 Mira 参考图，身份信号仍被参考视频运动信号压过，人物形态崩坏**无法靠 ref_images 救回**；真正救回靠「锚定首帧 + 后期换脸」。
- **ep1 主成片 = 锚定换脸版 `outputs/ep1_vo_mmh3_fs.mp4`**；白模"运镜增强版"降为实验资产，ep2/ep3 走 run_series.py(锚定+换脸)标准流程。
- ref_images 能力保留在 mmh3_engine.py(无害，可作未来实验开关)，但不要再用白模路线试图解决人物崩坏。

## 方向修正（用户 2026-09-10 要求）：不换脸，白模继续改进
- **推翻上一节「死刑」结论**。用户明确：不要走换脸路线，白模版保留、继续改进人物一致性。
- 改进策略（数据驱动，沿用 arcface 量化，基准 mira_front）：
  1. 关 Turbo 提步数（4步→默认步数）：Turbo 太糙是已知崩坏因素，零成本先试。
  2. 若步数不够：参考视频改用「干净 Mira 片段」（锚定版对应镜）而非白模自身块（避免把崩的人物当参考）。
  3. 仍不够：引入身份强约束（IP-Adapter / Mira 身份 LoRA），不换脸但强注入身份。
- 第一实验已启动：`python _fix4.py 4 --no-turbo`（后台，日志 _fix4_noturbo.log），arcface 对比 shot4（目标明显高过 0.331）。

## 优化生成速度（2026-09-10）
- 单卡 RTX 5070 Ti 16GB → 不能并行多 ComfyUI 实例提速（争显存反而更慢）。速度只能挤单镜推理。
- H3 帧数硬约束：必须 17n+5 网格（5/22/39/56/73/90/107...），任意值会被吸附（如 --frames=48→56）。
- 实测 Turbo 56帧 shot4 = 201s（3.35min/镜）。关 Turbo 30步预估 ~3倍慢（~10min），是速度杀手，白模改进勿用它。
- 主杠杆=减帧 + 保持 Turbo：`_fix4.py` 已加 --frames= 支持。快速模式用 --frames=56（2.3s 视频），质量/成片用 90帧。
- 纠错：`--disable-smart-memory` 实际会让模型不常驻、每次重载→更慢，勿加；`--fp32-vae` 是对 Wan2.2 防灰噪点强制的，H3 去此参数有出灰噪点风险，暂不动。
- ComfyUI 已重启清掉慢的关 Turbo 实验。

## H3草稿 + LTX-2.5放大 方案评估（2026-09-10）
- 用户方案：H3 低分草稿 → LTX-2.5 精修放大（二阶段提速）。
- 阶段1 H3 低分：`_fix4.py` 已加 `--res`/`--frames`，后台跑 `shot4 --res=640x352 --frames=56`（像素 1/2.6，预计 ~80-120s/镜，比 1024x576 的 201s 快）。
- 阶段2 LTX 放大三障碍：① LTX-2.5 upscaler 模型本地缺失（需下 2.5 专用，非 2.3 的 ltx-2.3-spatial-upscaler）；② `LTXEngine.generate` 的 `prev_clip` 是空壳，无 v2v/upscale，需新建 upscale 工作流+引擎方法；③ ComfyUI 有 `LTXVLatentUpsampler`(2x latent 放大)+`LatentUpscaleModelLoader` 节点可用，`workflows/ltx23_t2v_api.json` 可作改造模板。
- 跨模型身份硬伤：LTX 放大不会保留 H3 的 Mira 长相，最终仍需换脸统一身份（与既有结论一致）。
- 提速现实：单镜总耗时 = H3低分(快)+LTX高分(慢) >> 直接 H3高分(~5min)，**不会更快**；真正收益是「草稿快迭代验证 + 画质提升」。
- 替代（更简单保身份）：同模型 H3 低分→H3 高分精修，身份一致无需换脸，但单镜也不更快。
- 待用户决策：是否接受单镜不更快/需换脸，并授权下载 LTX-2.5 upscaler 模型开发阶段2。

## 用户选 B：同模型 H3 草稿→成片（2026-09-10）
- 决策：选 B（同模型 H3 低分草稿→H3 高分成片），保身份、免换脸、省一道 LTX。
- 实现：`_fix4.py` 加 `--outdir` 参数——草稿输出 `outputs/blocking/_draft/`，成片默认 `outputs/blocking/_fix/`，避免草稿覆盖成片（已发生：640x352 草稿覆盖了 _fix/04.mp4，已移到 _draft 复用）。
- 两步流程：① `python _fix4.py 4 --res=640x352 --frames=56 --outdir=outputs/blocking/_draft`（草稿，快）② `python _fix4.py 4 --res=1024x576 --frames=90`（成片）。同 seed(`BASE_SEED+1000+idx`)+同 prompt+同模型+同 ref → 身份天然一致。
- 阶段1 实测：640×352+56帧 Turbo → 04.mp4 310KB（6:46 完成）。
- 阶段2 成片完成：1024×576+90帧 Turbo → `_fix/04.mp4`，**342.7s**（首次含模型冷启动，比稳态 201s 慢）。同 seed(20261911)+同 prompt+同模型，与草稿身份天然一致——方案 B 端到端跑通。

## attention 后端排查（2026-09-10）
- 用户问 Windows+RTX5070Ti(sm_120) 用哪个 attention 方案更好。
- 现状：ComfyUI 启动 `main.py ... --port 8188 --fp32-vae`，无 attention 优化参数 → 默认 SDPA；已装 attention 库：无（pip list 空）。ComfyUI 内置 sage3/sage/xformers/flash/sdpa 五后端，启用靠 `--use-sage-attention`。
- 候选可装性实测：① `sageattention` 1.0.6 可装，但 import 需 `triton`，PyPI 对 Win+torch2.11 无 triton 包 → 死路，已卸载干净；② `sageattn3`(Blackwell 专用) PyPI 搜不到；③ `flash-attn` 2.8.3 可装但 2.x 只支持到 sm_90，RTX50 系是 sm_120 → 不支持；④ xformers sm_120 不支持。
- 结论：Windows+sm_120 上 **SDPA 是唯一可用**后端（torch2.11 SDPA 已自动用 flash/mem-efficient 内核，并不慢）——所有更快的量化 attention 都被平台/架构卡死。
- 真正提速点：当前启动带 `--fp32-vae` 违反 memory「VAE 默认正确勿带 --fp32」建议，去掉可加速 VAE 解码。解锁 SageAttention3/FlashAttn3 需 **WSL2(Linux)** 跑 ComfyUI。
- 待决策：A 去 --fp32-vae 重启验证 / B 规划 WSL2 / C 保持 SDPA。

## H3/LTX 混搭 workflow 报错排查（2026-09-10）
- 用户贴 ComfyUI 报错（Node 70 KSampler: mat1 75x6144 × mat2 5120x5376 不匹配）。根因：GUI 里把 LTX-2.5 CLIP(gemma4-12b,输出6144维)接到 H3 UNet(minimax_h3_fl2va)上；H3 token_refiner(comfy/ldm/minimax/model.py:514)写死要5120维文本特征，H3 自有 TE MiniMaxH3TEModel_ 才输出5120。另两错配：VAE=Wan2.2_VAE、latent=EmptySD3LatentImage(图像)。
- 与方案B无关：日志证 _fix4.py 走正确H3路径，3次均成功(199.9s / 119.56s→_draft/04.mp4 / 341.22s→_fix/04.mp4)。
- 结论：H3 组件不可跨模型混搭（CLIP/VAE/latent 必须 H3 全套）；想用 LTX 用 LTX 全套。

## 【推翻上一节】SageAttention2/3 在 Win+sm_120 实测跑通（2026-09-10 晚）
- **纠正**：上文「attention 后端排查」判定「Windows+sm_120 上 SDPA 是唯一可用后端、triton 无解」**是错的**。当晚成功装上并实测 SA2/SA3。
- 两个卡点的正确解法：
  1. **triton**：PyPI 官方 triton 不支持 Windows，但社区版 **`triton-windows`** 支持，`pip install triton-windows` 装到 3.8.0（cp313），torch 未被覆盖。
  2. **wheel**：别碰 PyPI（`sageattention` 1.0.6 版本老旧且依赖缺失）。用 HF **`ussoewwin/Sage-Attention-for-Windows`**，含 SA2/SA3 各版本的 cp312/313/314 × cu130/cu132 × torch2.10~2.14 预编译 wheel。本机的完美匹配：
     - `sageattention-2.2.0+cu130torch2.11.0-cp313-cp313-win_amd64.whl`（SA2, 25MB）
     - `sageattn3-1.0.0+cu130torch2.11.0-cp313-cp313-win_amd64.whl`（SA3, 4.1MB）
  3. 安装必须 `--no-deps`（防 pip 覆盖 torch 2.11）；wheel 文件名**必须保留完整五段标签**，重命名为 `sageattn3.whl` 会报 `Invalid wheel filename`。
- 基准 `_bench_sage.py`（bf16, H=12, D=128, 对比 SDPA）：

  | L | sdpa | sa2 | sa3 |
  |---|---|---|---|
  | 512 | 0.07ms | 0.19ms (0.35x 慢) | 0.27ms (0.24x 慢) |
  | 2048 | 0.78ms | 0.18ms (**4.28x**, cos .9993) | 0.28ms (**2.79x**, cos .9823) |
  | 8192 | 10.93ms | 1.76ms (**6.22x**, cos .9992) | 1.55ms (**7.05x**, cos .9812) |

- 结论：① **短序列(L≤512) 别用 Sage，反而慢 3~4 倍**（量化开销 > 带宽收益），交叉点在 512~2048；② SA2 cos≈0.999 几乎无损、SA3 cos≈0.981 有可见量化损失（FP4 代价，与上游 README ~0.98 吻合）；③ SA3 仅在长序列略快于 SA2（7.05x vs 6.22x），但精度差一档。
- **对 H3 的取舍**：用户极在意人脸一致性，SA3 那 2% attention 误差很可能优先劣化人脸 → 优先选 SA2（几乎无损 6x），SA3 仅作极限提速备选。
- ComfyUI 启用机制（`comfy/ldm/modules/attention.py`）：
  - `--use-sage-attention` 启动参数 → `optimized_attention = attention_sage` = **SA2 全局生效**（第 855-857 行），H3 走标准 `optimized_attention`/`attn1_patch` 接口，能直接吃到。
  - SA3 是 `register_attention_function("sage3", attention3_sage)`（第 891-892 行）**注册式、按节点选**，不是全局参数 → 需要 KJNodes 的 `PatchSageAttentionKJ`（**本机未装 KJNodes**）或等效 patch 节点才能切。
- 启动脚本 `_launch_comfy.py` 当前 args 仅 `--port 8188`，加 `--use-sage-attention` 即可启用 SA2。

## Sage(SA2) 实拍验证 + 顺带修复引擎两个 bug（2026-09-10 晚）
- `_launch_comfy.py` 已加 `--use-sage-attention` 并重启 ComfyUI；启动日志 `mmh3_launch.err` 出现 **`[INFO] Using sage attention`** 即确认生效（注意：ComfyUI 的 logging 走 **stderr**，不在 .log 里）。
- 实拍 shot4（640×352/56帧/Turbo，ref_images=3）：首跑 **93.9s**（含 triton 首次 JIT 编译）→ 稳态 **86.6s**；同档位 SDPA 基准约 201s → **约 2.3x 加速**。低于纯 tensor benchmark 的 6x，因 H3 还有 VAE/音频/文本编码等开销，且真实 token 长度不及 8192。
- **修复 bug ①（ImageSharpen 参数名错误）**：`mmh3_engine.py` 后处理传的是 `"sharpen"`，但 ComfyUI 内置 ImageSharpen 需要 `sharpen_radius`/`sigma`/`alpha`（`comfy_extras/nodes_post_processing.py:189`）→ 必报 "Required input is missing"。已改为 `sharpen_radius=1, sigma=1.0, alpha=self.post_sharpen`（强度映射到 alpha）。
- **修复 bug ②（节点 ID 冲突）**：`ref_images` 的 LoadImage 用 `"2%d" % i` 占用 **20~28**（i=0..8），而 `post_upscale` 用 20/21、`post_sharpen` 用 22 → 后处理覆盖参考图节点，使 `ref_image_2` 指向 ImageSharpen，形成 `6←22←11←10←7←6` 依赖环，workflow 直接 400。已将后处理改到 **30/31/32**。
- 触发条件：`ref_images ≥3 张` 且 `config.engine.comfyui_mmH3.post.sharpen > 0`（当前 0.2）时必炸。修完 `_fix4.py` 正常出片（543KB）。

## SA2 vs SDPA 严格对照（同参数热启动，2026-09-10 晚）
- **修正上一节的高估**：先前引用的「基准 201s」含模型冷启动，**不可比**。严格对照（同 workflow：640×352/56帧/Turbo/ref_images=3/sharpen=0.2，均热启动）：
  - SDPA **113.1s** → SA2 **86.6s** = **1.31x 加速**（省约 23%），非 2.3x。tensor benchmark 的 6x 只反映纯 attention 层，H3 实拍还有 VAE/音频/文本编码开销。
- **arcface 画质对照**（`_cmp_sage_quality.py`，基准 mira_front，每片抽 8 帧）：
  - Sage(SA2) 平均 **0.3509**（最低 0.3153，检出 8/8）
  - SDPA 平均 **0.3631**（最低 0.3254，检出 8/8）
  - 差 **0.012**（相对 3.4%），在抽帧噪声量级 → **SA2 对人脸的劣化可视为噪声级，可安全常态化**。
  - 注意：两版绝对值都仅 ~0.35（偏弱），这是 H3 未换脸本身的水平（换脸后可到 0.466/0.85），**不是 attention 后端造成的**。
- `diag_face_consistency.py` 是硬编码对比 before/after 两目录的，不能比任意两视频；故新建 `_cmp_sage_quality.py`（传任意多个视频路径即可比）。
- `_launch_comfy.py` 改为**环境变量控制**：`COMFY_SAGE=0` 关闭 Sage，默认（1）开启——切换后端无需改代码、重启即可。ComfyUI 当前已恢复 Sage 模式。
