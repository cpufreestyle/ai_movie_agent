# 2026-09-08 工作日志

## MiniMax H3 文生视频接入成片管线（已完成）
- 模型 INT4 量化档 + Turbo 4step LoRA 已落地（见 MEMORY.md ④）。
- 引擎 `agent/mmh3_engine.py`(`MMH3Engine`)、CLI `python cli.py mmh3 ...`、三集出片 `run_series.py --engine mmh3`；配置 `config.engine.comfyui_mmH3`。
- 引擎自动修正 H3 两约束：width/height 被 32 整除、总帧数吸附到 `17n+5`。

## 第 1 集成片（已完成）
- `run_series.py --engine mmh3 --ep 1` 跑通 18 镜，总耗时 ~11 分钟。
- 产物 `outputs/ep1_series_film_mmh3.mp4`：33.5s / 768×448 / 24fps / **aac 立体声 @48kHz(11.16MB)**。H3 原生立体声经 acrossfade 拼接，成片直接带音、无需垫旁白音轨。
- 日志 `outputs/series_h3_ep1.log`。

## 第 1 集加英文旁白 + 双语字幕（已完成）
- `make_narration.py` 新增 `--fit-film`（统一变速+顺排填满成片）与 `--fps` 两参数。
- 第1集自然旁白 47.5s ≫ 成片 33.5s，**统一 1.44x 变速**顺排，9 段无重叠、不截断。
- 命令：`make_narration.py --film outputs/ep1_series_film_mmh3.mp4 --out outputs/ep1_vo_mmh3.mp4 --series-script outputs/series_script.json --ep 1 --fit-film --fps 24 --auto-dur --orig-vol 0.18`
- 产物 `outputs/ep1_vo_mmh3.mp4`：33.50s / 768×448 / 24fps / aac 立体声；原 H3 音轨压 0.18 垫底，英文旁白(Andrew)在上层，双语字幕(上英下中)烧录。
- 坑：`silenceremove` 去静音在本环境过度裁剪(把旁白截成碎片)→ 已弃用，仅靠统一变速匹配时长。

## 待办 / 可选
- 第 2、3 集 H3 生成：`run_series.py --engine mmh3`（各 ~11 分钟）。
- **第 1 集拉长到 ~1 分钟（已完成，已同步）**：后台作业 `run_ep1_60s.bat`(PID 36336) 在 14:39 跑完 ep1=90帧/镜→18镜×fade0.5=59s，并叠 VO。但**同步 bug**：旧 `--fit-film` 是「顺序平铺」(每段按自然时长累积排)，起点与「2镜1段」镜头块错位→不同步。`make_narration.py` 已改为**镜头块对齐**(每段起点=镜头块起点、贴合该块时长)，修正后验证 59s 版每段 1.0x 自然语速且对齐。已把对齐版 `ep1_vo_test.mp4` 复制为 `ep1_vo_mmh3.mp4`(旧平铺版被覆盖)。
- **第 2、3 集 60s（已完成，已同步）**：`run_ep23_60s.bat`(detached PID 10856) 已跑完。ep2 成片 15:36:56、VO 15:37:10；ep3 成片 15:51:49、VO 15:51:58。两集均走镜头块对齐（日志 `[timing/align] 18镜/9段, 每镜3.750s, 每段≈6.50s`，变速 1.0–1.03x）。三集 VO 成品：`ep1_vo_mmh3.mp4`(17.46MB)/`ep2_vo_mmh3.mp4`(16.20MB)/`ep3_vo_mmh3.mp4`(16.31MB)，均 59.00s/768×448/24fps/h264+aac 立体声。`outputs/review.html`(python -m http.server 8777 预览) 三集并排回看。
- **同步修正要点**：对齐触发条件 N_SHOTS%n_segments==0（18镜/9段=2:1）。T_actual=(total+(N_SHOTS-1)*X)/N_SHOTS；slot=sps*(T_actual-X)；每段超 slot 则压缩、不足 1.0x 留隙。
- 若想旁白 1.0x 自然语速，需把第1集重渲染为更长成片（如 72 帧/镜 ≈ 45s），再走同等 `--fit-film`（此时 G≈1.0，无需变速）。
- 对比 LTX 版：`outputs/ltx23_film.mp4` / `outputs/ltx25_film.mp4`。

## 看板接入 WebUI（已完成）
- 临时回看页 `outputs/review.html` + 8777 预览服务**已删除**，功能并入 WebUI 新页签「看板」（**默认首页**，`python webui.py` → http://127.0.0.1:8000）。
- 后端 `webui.py` 新增：`/api/board`（三集规格/镜头数/台词/同步状态）+ `probe_media()`（ffmpeg 探时长/分辨率/fps/编码，按 (path,mtime,size) 缓存）+ `series_script()` + `EP_TITLES`。判据「同步」= `shots % narration == 0`（与 make_narration 镜头块对齐一致）。
- 前端 `webui/pipeline.html`：nav 加「看板」按钮并置为默认 active；新增 `#tab-board` + `.board-grid/.ep-card` 样式 + `boardCard()/loadBoard()/publishEp()/openFilm()`。
- 每集卡片：内嵌播放器(`/api/film/play`，Flask `conditional=True` 已支持 Range/206) + 画面/旁白/字幕/同步徽章 + 规格网格 + 台词折叠(英/中对照) + 「投稿这集」「在成片页打开」按钮。
- 实测：3/3 已出片，总时长 177s / 47.66MB / 54 镜 / 27 段；每集 59s·768×448·24fps·h264/aac stereo。
- 坑：改完 `webui.py` **必须重启服务**才生效（Flask debug=False 无热重载）；旧实例请求新路由返回 404（curl 拿到空 body 易误判为服务没起）。另 curl 测本机建议加 `--noproxy '*'`。
