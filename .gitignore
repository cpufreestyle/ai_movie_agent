# 本地配置：含 api_key，且 WebUI 的「接口与模型设置」会写回该文件。
# 本仓库为公开仓库 —— 一旦提交即等于公开泄露密钥。仓库只保留 config.example.yaml。
config.yaml
config.local.yaml

# 旧版 CodeBuddy 的记忆目录：内容已于 2026-09-15 并入 .workbuddy/memory/，
# 这里只保留磁盘备份，不再入库（避免两份互相冲突的记忆同时存在）。
.codebuddy/

# 虚拟环境
.venv/
.venv-win/
env/
venv/

# Python 缓存
__pycache__/
*.pyc
*.pyo

# 生成的产物（渲染视频、state.json 等运行时输出）
outputs/
outputs-windows-test/
output/

# 系统文件
Thumbs.db
.DS_Store

# 本地日志
*.log
.github_ssh_known_hosts

# 第三方发布工具（运行时通过 tools/_install_biliup.py 安装，不入库）
tools/biliup/

# Blender 便携版（运行时下载解压，体积大不入库；用 tools/_install_blender.py 安装）
tools/blender/

# runtime logs / artifacts / backups (not tracked)
*.err
*.bak
diag_*.png
comfy_run.log

# 创空间部署时的临时克隆（内含嵌套 .git，不能进索引）
space/_deploy_tmp/

# one-off dev scripts (underscore prefix)
# 注意：`_*.py` 同样会命中 `__init__.py`（`*` 能吃掉 `_init`），会把整个包的
# 包声明文件一起忽略掉 —— 本地因为有文件而「看起来正常」，但 CI 全新克隆时
# 会缺 webserver/__init__.py，`from webserver import register_blueprints` 直接
# ImportError（namespace package，unknown location）。故必须显式取反放行。
_*.py
_*.ps1
_*.bat
_*.txt
!**/__init__.py
kill_test.py
tmp_vae_test.py

# 测试 / 构建缓存
.pytest_cache/
# 本地 pytest 临时目录（放仓库内可避开沙箱对系统临时目录的清删拦截）
.pytest_tmp/
.ruff_cache/
*.egg-info/
build/
dist/

# 本地测试 / 截图临时目录（agent-browser 自测产物）
.workbuddy/tmp/
