# 2026-09-15

## WebUI 主题化改造（亮/暗 · 密度 · 强调色 · 导航分组）+ 修复 <style> 未闭合 bug
- 起因：用户选「全部四项」——亮/暗切换、页签布局、间距密度、主题色。
- **修复真 bug**：上一批美化层用 `</style>\n</head>` 做替换时把原 `<style>` 的闭合标签一并吞掉 → 页面变成 2 个 `<style>` 开标签 / 1 个闭标签，favicon 被关进样式里失效、`id=ui-aesthetic` 名不副实（浏览器容错所以视觉正常）。修法：在 `<link rel="icon">` 前补回 `</style>`。timeline.html 当时 new_string 带了 `</style>`，无此问题。
- 新增 `<style id="ui-theming">`（pipeline.html）：
  - 亮/暗：`html[data-theme=light]` 覆盖 `--bg/--surface/--surface2/--line/--fg/--mut/--green/--red/--shadow`，并覆盖硬编码色（`.stage-head/.ep-head:#171c22→#f3f7fc`、input/textarea/select/`.chip:#151a20→白`、`pre:#0d1014/#cdd8e1→浅`、video 保持黑底、header h1 渐变改深、滚动条、hover 叠加改 `rgba(0,0,0,*)`）。
  - 强调色：`html[data-accent=*]` 驱动 `--blue`（blue/green/purple/amber/pink/cyan）；nav 激活渐变用 `color-mix`（不支持则回退旧渐变）。
  - 密度：`html[data-density=compact]` 收紧 main/topbar/stage/panel/nav 间距，默认「舒适」。
  - 导航：`nav{position:sticky;top:0;z-index:30}` + `@supports` 半透明毛玻璃；`.nav-sep` 分隔线把 9 个页签分成 4 组（制作/阶段/产出/设置）。
  - 外观控件：头部 `.appearance`（`.seg` 分段按钮 ×2 + `.swatches` 色板 ×6）。
- 防闪烁：`<head>` 内早期脚本读 localStorage `ui-theme/density/accent` 设 `data-*`；切换后写回并同步 `.on` 高亮。
- timeline.html：加 `ui-theme` 块 + 早期脚本，跟随主应用偏好（读同一 `ui-*` key，无自带控件）。
- 坑：色板初版把颜色写在 `data-c` 上用 `var(--c)` → CSS 读不到（`attr()` 不能用于 background），改内联 `style="--c:#..."`。
- 交付/验证：`<style>` 3 开 3 闭；CSS 花括号配平（pipeline 190/190、timeline 52/52）；自写 HTMLParser 校验标签 0 错误 0 残留；Flask（托管 Python）`/`、`/timeline`、`/api/overview` 均 200 且含新标记。
- **环境坑**：`D:/Program/python.exe` 在沙箱重置后消失 → 托管 venv 缺 flask，`pip install --proxy http://127.0.0.1:11268 flask pyyaml requests` 装上后用托管 Python 起服务。

## 安装浏览器自动化 + WebUI 端到端自测（agent-browser）
- 装法（隔离，不污染全局）：`node <npm-cli.js> install agent-browser --prefix C:/Users/michael/.workbuddy/binaries/node/workspace --registry https://registry.npmmirror.com`。npm 用托管 node 直调 `node_modules/npm/bin/npm-cli.js`（PATH 上的 `npm` shim 会乱码并触发 wsl 黑名单）。
- **关键：不自带 Chromium**（`agent-browser install` 从 Google CDN 下载慢/易卡）→ 改指向系统 Chrome：`export AGENT_BROWSER_EXECUTABLE_PATH="C:/Program Files/Google/Chrome/Application/chrome.exe"`，并 `export NO_PROXY=127.0.0.1,localhost`（否则 localhost 走代理 502）。CLI 入口：`node <ws>/node_modules/agent-browser/bin/agent-browser.js <cmd>`。
- **坑**：① 不要把命令写成 `.sh` 用 `bash` 跑（shim 触发 wsl.exe 黑名单，整条失败）；用**内联命令**。② agent-browser 会话在同一 Bash 调用内持久（daemon），**跨 Bash 调用不保证**——有状态交互（open→click→截图）必须在**同一次**调用里完成，否则在空白页点击报「Element not found」。③ 输出含 ANSI，重定向成日志后会被判二进制；写个小工具 strip `\x1b\[...` 再读。
- **端到端测试结论（全绿）**：9 个页签逐一 `click nav button[data-tab=…]` 后 `nav button.active` 均正确切换；主题：清 localStorage 后 `data-theme=null`/body `rgb(17,20,24)`（默认暗），点「亮」→ `light`/`rgb(238,241,246)` 且写入 localStorage，切回「暗」恢复；密度 compact → `main` padding `12px`；强调色 purple → 导航激活渐变变 `#8b5cf6`（`color-mix` 生效）；`/timeline` 自动跟随为 light；`errors`/`console` 均为空（**无 JS 报错**）。
- **visual 校验法（重要）**：不要靠肉眼判断截图主题——本地 Read 图片渲染会给了陈旧/错误画面，误判成「浅色」。改用 Pillow 测平均亮度：暗≈(29,35,41)，亮≈(243,245,248)；并用 md5 比对截图是否真变。据此确认默认暗、切换生效（我一度据此纠正了误判）。
- 测试产物：`.workbuddy/tmp/uitest/*.png`（A-dark / B-light / C-white-light / D-white-dark / 03-film / 06-light-purple-compact / 07-timeline-light 等）。

## 推送通道切换 + 环境抖动收尾
- 提交 `fe2e98e`（gitignore + 记忆）时：11268 代理挂（`Could not connect` / `CONNECT tunnel failed 502`），直连被墙，最终走 **7897 代理 + 保留默认 credential.helper** 推送成功（约 2min）；禁用 helper 会报 `could not read Username`。已把「按序试代理 + 按报错判状态」写进 MEMORY.md。
- 期间沙箱出现过 ~2 分钟的 DLL 初始化失败（`0xC0000142`，连 `echo` 都挂），随后自愈。
- WebUI 预览服务被回收后已用托管 Python 重启（:8000，后台）；HTML 即时生效，无需重启即可反映主题化改动。

## 专业选项「悬停解释」tooltip（webui/pipeline.html）
- 需求：专业选项（白模模块 + 设置页阶段选择）鼠标悬停时弹解释。
- **实现方案（避免 overflow 裁剪，优于纯 CSS `::after`）**：单一浮层 `#ui-tip`（`position:fixed`，`z-index:2147483000`，`pointer-events:none`，`white-space:pre-wrap`）由 JS 按 `[data-tip]` 定位；`document` 级 `mouseover/mouseout/focusin/focusout` 事件委托，`closest('[data-tip]')` 取最近目标；越界自动左/上翻边（`getBoundingClientRect` + `offsetWidth`）。
- 视觉可发现性：内容文本包 `<span class="tip" data-tip="…">`，加虚线下划线 + 圆形「?」小徽标，hover 变主题色；`[data-tip]{cursor:help}`。
- **关键可用性修正**：只给 `<span>` 加 `data-tip` 时，悬停 `<select>`/`<input>` 本体不触发（实测 light 主题 hover `#blk-engine` 时 `#ui-tip` 类名为空）。修法：IIFE 里把 `span.tip[data-tip]` 的值**复制到外层 `<label>`**（仅当父级是 LABEL 且自身无 data-tip），DOM 中 `[data-tip]` 元素 23→45，整块控件区都可触发。
- 多行文案用 HTML 实体 `&#10;` 写在属性里（源文件保持单行，浏览器解析成真换行 + pre-wrap 渲染）。
- 覆盖范围：白模模块 6 个参数 + 4 个开关 + 开关组标题 + 分镜文本 + 渲染模式；设置页 A~H 共 10 个阶段档案选择器。合计 23 处 `data-tip`。
- 验证：HTMLParser 0 错误、无未闭合；`<style>` 3/3、ui-theming 花括号 64/64；服务端 200 且已含新标记；agent-browser 逐页签切换 `errors` 为空；暗/亮主题各 hover 截图确认浮层渲染与可读性。

## 发布 v0.11.0 Release（首次打通 release 链路）
- 现状：远端只有 `v0.9.0`/`v0.10.0` 两个 tag（**本地从未 fetch，`git tag` 为空**）；仓库**无任何版本文件**（无 pyproject/package.json/VERSION/CHANGELOG），版本号只存在于 tag。
- **本机无 `gh` CLI** → 改用 GitHub REST API：token 从 `git credential fill`（`password=` 行）取，脚本内只打印 bool 与长度，**不落盘不打印**；API 走代理 `127.0.0.1:7897`。
- 增量：`v0.10.0..HEAD` = **44 提交 / +16853 −1603 行 / 83 新文件**（v0.10.0 = `001bc53`，2026-09-10；HEAD = `32f1e8d`）。
- 收集到的事实（写 notes 前逐一核对，不凭印象）：`webui.py` 1058→78 行；`cli.py` 268→594；`agent/blocking.py` 285→987；`config.yaml` 新增顶层键 `qa`/`preflight`/`prompting`/`series`；CLI 18 个子命令（`COMMANDS` 表）；webserver 38 个路由装饰器；tests 30→**128** 例（13 文件）。
- notes 沿用上一版标题层级：`## 亮点` + `## 工程 / 质量`（+ 本版补 `## 升级提示`）。内容分 11 条亮点 / 8 条工程质量，全部带可核对数字（9.92s→1.24s、1.7×、28 处死代码、74→<10 等）。
- `POST /releases`（`tag_name=v0.11.0`、`target_commitish=main`、非 draft/prerelease）→ **201**；自动成为 latest。校验：tag 指向 `32f1e8d` == 本地 HEAD；body 3067 字符无缺失片段。
- URL：https://github.com/cpufreestyle/ai_movie_agent/releases/tag/v0.11.0
- **已把整套流程固化为 skill `github-release-no-gh`**（`~/.workbuddy/skills/`），下次发 release 直接复用。

## 全项目体检（发现 2 个真隐患）
- **健康面（实测）**：`ruff check .` **全绿**（rc=0，符合 ruff.toml 门槛）；`pytest -q tests/` **229 passed / 0 failed**（40.5s，装齐 requirements.txt 后）；`git status` 干净；受控文件 212 个 / **仅 1.7 MB**，无 >1MB 大文件；CI 含 compileall + ruff + smoke + pytest 且无 continue-on-error。**无凭据泄漏**：全仓库扫不出真实 key（`tests/fixtures/mmh3_wf_fingerprint.json` 里的 64 位 hex 只是工作流指纹哈希；config.yaml 仅 `api_key: ollama` 占位，git 历史里也从未出现真实 key）。
- **隐患①：记忆目录分叉（严重，项目卫生）**。`.codebuddy/memory/`（17 文件，含 **09-01~09-13 共 13 天历史**）与 `.workbuddy/memory/`（3 文件，仅 09-14/15 + MEMORY.md）**两者都被 git 跟踪**；三个同名文件（09-14、09-15、MEMORY.md）**内容全不相同**（例：09-14 为 7406 vs 16204 字符）。当前 AI 只读 `.workbuddy/memory/`，故 **09-13 之前的历史对它不可见**，且重叠日期存在两份互相冲突的版本。修法：把 `.codebuddy/memory/*` 并入 `.workbuddy/memory/`（`.workbuddy` 为唯一权威）、迁移 `.codebuddy/automations/ltx-2-5/memory.md` 后停跟踪 `.codebuddy/`。
- **隐患②：`config.yaml` 入库 + WebUI 会写回 = 密钥泄漏定时炸弹**。config.yaml 受版本控制且**不在 .gitignore**；WebUI「保存接口与阶段选择」→ `POST /api/config` 直接写盘该文件。目前只有 `ollama` 占位**尚未泄漏**，但一旦在 UI 里填真实 key（StepFun/Kimi/OpenAI…），下次 commit+push 就会把 key 公开到 public repo。修法：`git rm --cached config.yaml` + 加 .gitignore，改跟踪 `config.example.yaml`（或让 `api_key` 只从环境变量/`config.local.yaml` 读）。
- **中等**：10 处圈复杂度 >10（ruff.toml 未开 C901，CI 不拦；记忆里的目标本是 <10）——`agent/preflight.py:check` 22、`agent/ltx_engine.py:_inject` 20、`agent/concept_video.py:render_concept_video` 19、`ltx_engine._strip_cloud_prompt_branch` 17、`agent/publisher.py:upload` 15、`mmh3_engine._build_workflow` 14、`concept_video._write_mp4` 12、`_encode_frames` 11、`mmh3_engine._validate_control_video` 11。另有 `README_WINDOWS.md:31-32` 的 MinimaxH3 段落**已过期**（仍称 G 阶段调 SkyReels 的 `generate_video_df.py`、run/pipeline 不出片——实际 H3 已是默认引擎且正常出片）。
- **低**：`webui/index.html` 死文件（受控但无任何路由/代码引用，webserver 只用 pipeline/timeline/ep4_film/ep5_film.html）；`_exp_ref_video.py` 被跟踪却与 `.gitignore` 的 `_*.py` 规则冲突；`legacy/download_ltx23_q3.py` 全仓库零引用。
- **文档引用检查教训**：第一版 matcher 只按 basename 匹配，报出 63 处"失效引用"，几乎全是误报（`writer.py` 实为 `agent/writer.py`、`app.py` 实为 `space/app.py`、`run_wan22_*.py` 实为 `legacy/…`、`tripo_gen.py` 是计划稿里已标注「废弃不做」的）。**必须先按仓库相对路径匹配、再退化为 basename 全局匹配**，否则会得出错误结论并误导用户。

---

## 附：旧目录 `.codebuddy` 同日记录（并入时保留，内容与上文互补）

## 固化 lint 基线 + webserver 配置缓存（承接上一轮 P 系列收尾）
- 新增 `ruff.toml`（仓库首次有固化配置）：`line-length=100`、`target-version=py310`，
  `select = ["E9","F63","F7","F82","F"]`（只开不会误伤风格偏好的规则），`exclude` 覆盖默认项 + `legacy`/`outputs`。
  暂不开 E4/E7（52 处）：E402 与本项目「刻意延迟 import 重依赖」冲突；E722/E741 属风格级。
- `.github/workflows/ci.yml`：Lint 步骤由 `ruff check . --select E9,... --exclude legacy`（曾带 continue-on-error）
  改为 `ruff check .`（吃仓库配置），本地与 CI 行为一致。
- 清掉 28 处 lint（F401 未用导入 / F541 无占位 f-string / F841 未用局部变量 / F811 yaml 重复导入）。
  ⚠️ `tools/blender_server.py` 的 `import bpy` 被 ruff --fix 误删 → 已恢复并加 `# noqa: F401`：
  `_exec()` 是 `exec(code, globals())`，远端脚本依赖模块全局的 `bpy`（blocker 模板虽自带 import bpy，但其它客户端不一定带）。
- `webserver/state.py`：`load_config()` 改为按 `(mtime_ns, size)` 指纹缓存（实测 safe_load 13KB≈10.8ms vs deepcopy≈0.10ms；
  原实现每次请求解析多次 + 前端 1s 轮询 `/api/logs`），**返回 deepcopy** 保留「每调用方独立对象」语义（MovieAgent/Publisher 会持有并改写 config）；
  新增 `invalidate_config_cache()`。`webserver/views/core.py` 的 `save_config()` 写回后调它，防改了配置仍读旧值。
- 新增 `tests/test_config_cache.py`（7 例）：独立副本、同指纹只解析一次、文件变更/显式失效后重解析、缺失文件不填脏缓存、deepcopy 保结构。
- 验证：`ruff check .` 全绿；`pytest tests` 207 passed；`python tests/smoke_test.py` 50 pass 0 fail；`compileall` rc=0。
- 提交 `a73648d` 并推送（eec1c5e..a73648d main→main，ls-remote 已核验远端 SHA 一致）。

## 环境坑：本机 7897 代理已不通 GitHub，需直连
- 现象：`git push` 报 `schannel: failed to receive handshake`；换 `http.sslBackend=openssl` 报 `unexpected eof while reading`。
- 排查：`curl.exe -x http://127.0.0.1:7897 https://github.com` → 000（代理已失效）；`curl.exe https://github.com` → 200（直连通）。
- 解法（不改 git config，按次覆盖）：`git -c http.proxy= -c https.proxy= push origin main`。
  注意：`git ls-remote` 同样要带覆盖才不报错；PowerShell 里 `curl` 是 Invoke-WebRequest 别名，要用 `curl.exe`。

---

## 复杂度治理收尾：48 处 C901 → 0，并把 C901 纳入 ruff/CI 门槛（commit 07c8204）
- 起因：体检时只估到 9~10 处 >10，实测开 `C901(max=10)` 后全仓 **48 处**（其中 17 处在 `tools/blender_mcp_addon.py`，上游 vendored）。
- 结论：**除 2 个例外全部真实拆分**，例外走 `[lint.per-file-ignores]` —— `tools/blender_mcp_addon.py`（随上游同步，不为风格分叉）、`deploy/sol_h3_spark/sol_h3_server.py`（部署到远程 GPU，脱离本仓库维护链路）。
- `ruff.toml`：`select` 加 `C901`，新增 `[lint.mccabe] max-complexity = 10`。
- 拆分清单（手法统一「抽小函数」，不改行为/接口）：
  - `agent/`：`preflight.check` 22 → `_check_video/_check_title/_check_tags/_check_cover`；`ltx_engine._inject` 20 + `_strip_cloud_prompt_branch` 17 → 11 个模块级图节点函数（`_pick_node/_set_in/_inject_*/_rewire_*`）；
    `qa.evaluate` → `_RANGE_CHECKS` 表 + `_num`；`mmh3_engine._build_workflow` 14 → `_add_model_chain/_build_cond_inputs/_add_first_frame/_add_ref_video/_add_ref_images/_add_sampler/_add_fun_control/_add_output_nodes`；
    `publisher.upload` 15 / `update_video` / `publish_concept`；`concept_video.render_concept_video` 19 → 15 个 `*_card` 卡片构造器。
  - `webserver/`：`views/pipeline.api_pipeline_stage` 16 → `_stage_a~_stage_h` + `_STAGE_HANDLERS` 派发表；`views/run.api_run_full`；`services/pipeline.storyboard_from_bible` / `probe_media`。
  - 根脚本与 tools：`run_series`（`run_episode` 20、`main` 21）、`make_narration.build_and_render` 15、`face_swap_ep.main` 23（内嵌 `swap_video` 13 提升为模块级并收参）、`config_env`、`anime_redraw`、`upscale_video`、`deploy`、`monitor_upload`、`download_ltx25_gguf`、`tools/blender_mcp`、`tools/comfyui_workflow_conv`、`tools/verify_blocking_render`。
- 顺带修正：`mmh3_engine._add_ref_images` 的 `has_refimg` 循环判定改 `any(...)`（等价更短）。
- 验证：`ruff check .` 全绿；`pytest -q tests/` 230 passed；**等价性抽查** `run_series.py --help` 与 `git show HEAD:run_series.py` 逐字比对，除 prog 名外零差异。
- `.gitignore` 顺带补 `.pytest_tmp/`（仓库内 basetemp）与 `_*.txt`（一次性核查输出）。

## **CI 其实已经红了好几轮** —— 修掉两层串联的隐藏失败（commit d01f900 + 8b19ca7）
> 教训：CI 的「lint 绿」是假象，前一步失败会让后一步 `skipped`，看板只显示整体红但没人知道测试压根没跑。

### 第一层：`.gitignore` 的 `_*.py` 把 `__init__.py` 一起吃掉了
- 症状：spoke 脚本步骤报 `ImportError: cannot import name 'register_blueprints' from 'webserver' (unknown location)`。
- 根因：`_*.py` 里的 `*` 能匹配 `_init`，于是 **`webserver/__init__.py` / `services/__init__.py` / `views/__init__.py` 三个包声明文件从未入库**；本地磁盘上有所以一直绿，只有全新克隆才炸。`webserver/__init__.py` 正是导出 `register_blueprints` 的地方。
- 修法：在 `_*.py/_*.ps1/_*.bat/_*.txt` 之后加 `!**/__init__.py` 显式取反（其余 46 个 `_*` 一次性脚本仍照常忽略）；`git add` 补入三个文件。
- **复查法**：`git check-ignore -v <path>` 逐个确认；别只看 `git status`（被忽略的文件根本不出现）。

### 第二层：黄金指纹样本跨平台不可复现
- 症状（上一步修完才暴露）：`tests/test_node_ids.py::test_mmh3_workflow_golden_fingerprint` 失败，`classes_diff={}` 说明节点类没变、只是 sha 变了 → 有**字符串值**漂移。
- 根因：工作流里有两处真实绝对路径 —— `VHS_LoadVideoPath` 的 `ref_video` 与 `fc_video`（`mmh3_engine._add_ref_video` / `_add_fun_control` 走 `os.path.abspath`）；而测试的 `_norm` 只用 `TMP_RE` 掩掉 `golden_xxxxxx` 随机后缀，**父目录与路径分隔符原样保留** → 样本只在生成它的机器（Windows `E:\Temp\golden_xxx\...`）成立，Linux CI（`/tmp/golden_xxx/...`）必然不匹配。
- 修法：`_norm(o, tmp)` 两级归一 —— ① 整个临时目录前缀 → `TMPDIR`（`TMP_RE` 降级为兜底）；② `\` → `/`。`_fingerprint(wf, tmp)` 携带 `tmp`。样本重生成（56 case，仅含 ref_video/fc_video 的 case sha 变化，其余保持原值 —— 反向印证改的正是路径那一处）。
- **两道自检（都该有，别省）**：
  1. **重构中性**：与拆分前的 `mmh3_engine`（`git show bd977ce:agent/mmh3_engine.py > agent/_pre_mmh3.py`，`_*.py` 已被忽略、不会入库）跑同一矩阵，新归一化下 56/56 逐 case 同 sha → 证明复杂度拆分对工作流结构零影响。
  2. **跨平台可复现**：把 `tempfile.tempdir` 换成另一个根目录重跑，指纹完全不变 → 证明前缀确已掩净。再补一道：正则扫归一化后的 JSON，确认无盘符 / 反斜杠 / 真实 tmp 路径残留。
- **`git archive` 复刻 CI（判断红灯是不是自己引入的最可靠手段）**：`git archive --format=zip -o out.zip HEAD` → 解到干净目录 → 用托管 Python 跑 `tests/smoke_test.py` 与 `pytest -q tests/`。比 `git stash` 可靠（后者受本地残留文件影响，正是本次两个 bug 都是「本地绿、CI 红」的原因）。

### 无 gh CLI 查 CI（已写进 skill `github-release-no-gh`）
- `GET /actions/runs?per_page=10` 按 sha 找 run；`GET /actions/runs/<id>/jobs` 看**每个 step** 的 conclusion（定位远比整体红绿有用）。
- 拿日志的坑：`/actions/jobs/<job_id>/logs` 会 302 到 Azure 签名 URL，urllib 会把 `Authorization` 带过去 → **403 Server failed to authenticate**；需自定义 redirect handler，跳到非 `api.github.com` 时摘掉该头。返回体**单 job 纯文本、多 job 是 zip**，用 `blob[:2]==b"PK"` 判断。
- 结果：**8b19ca7 CI 全绿**（py3.10 + py3.12，8 步全 success）—— 这是本项目 CI 久违的一次真绿。
- 推送通道：本次 **7897 可用**（`git -c http.proxy=http://127.0.0.1:7897 …push origin main`），11268 已挂（`Could not connect`）。注意 `| tail` 会因 shim 缺 `tail` 报错并可能吞掉输出 → 一律重定向到文件再 Read。

## 锚定资产 WebUI 一键生成 + 又一层「本地绿 / CI 红」

### 需求确认
- 用户说「默认生成三视图」，经追问确认两点：**WebUI 加一键生成**（不是改脚本默认参数）、
  **只要 H3 版**（`gen_anchor_assets.py` → `outputs/anchor/`，不是 anime/ComfyUI 那条）。

### 落地（提交 e7ea21b）
- `webserver/services/anchor.py`：`SHOT_SPECS`（3 三视图 + 7 场景图）、`default_names()` = 三视图、
  `comfy_api()` 走 `H3_SECTION_ALIASES`/`pick_engine_section`、`comfy_ready()` 单次短超时探测、
  `artifacts()`/`shot_groups()`/`derived_items()`、`safe_path()`。
  **刻意不 import `gen_anchor_assets`**：它顶层 `import run_series`，会在 Web 服务启动路径上建目录 + 探 ffmpeg。
  代价是两边各存一份清单 → 由 `tests/test_anchor_assets.py` 断言**同名同序**兜漂移（漏一张图在界面上是静默的）。
- `webserver/views/anchor.py`：`/api/anchor`、`/api/anchor/run`（`only` 走白名单过滤 + `_state["running"]` 忙锁 409）、
  `/api/anchor/file`、`/api/anchor/index`（回传脚本生成的图册；无则 404，前端据 `has_index` 隐藏入口 ——
  别在 HTML 里直接写 `/outputs/...`，那条没有路由会 404）。
- `gen_anchor_assets.py`：加 `--force`（默认跳过已存在的 png，只补齐缺失）。
- `webui/pipeline.html`：nav 加「锚定资产」页签；页签含分组勾选（三视图默认选中）、分辨率下拉、
  「重新生成已存在的图」、一键生成、产物图册、任务日志；页签切换与任务结束都自动刷新。
- 路由基线 `tests/test_webui_routes.py` 由 35 → 39 条。

### CI 红 → 修（真因：路径穿越防护跨平台不一致）
- **e7ea21b 推送后 CI：pytest 1 failed / 247 passed**，唯一失败
  `test_safe_path_rejects_traversal[..\\config.yaml]`（ubuntu 上 `os.path.normpath` 把该串当普通文件名放行）。
- 根因一句话：**反斜杠在 Windows 上是路径分隔符、在 POSIX 上只是普通文件名字符** → 同一个 guard
  在 CI 放行、在本机拒绝。这已是本项目第三次「本地绿 = CI 红」，且每次原因都不同。
- 修法（提交 4cbfb4c）：`state.safe_under(base, name)` —— **先 `\`→`/` 归一化再 `normpath`**；
  `services/anchor.safe_path` 与 `views/blocking.py` 的 `/api/blocking/file`（**同一个洞**）都改成共用它。
- **本机是 ntpath，平台相关失败在 Windows 上验不动 → 用 `posixpath` 模拟另一套语义自证**：
  `posixpath.normpath(posixpath.join(base, rel))` 跑同一组输入，6 条越界串全 REJECT、2 条正常名全 KEEP。
  这个手法值得固化成习惯：凡「CI 红但本机绿」先问「这条断言依赖路径/编码/换行的平台差异吗」。
- 4cbfb4c 推送后 **CI 全绿**（py3.10 + py3.12，全 step success）。

### 环境（本次又变了）
- **代理端口翻转**：本次 **直连可用**（`git -c http.proxy= -c https.proxy= push origin main`），
  7897 反而变成 `schannel: failed to receive handshake`。→ **别记死端口，先扫**：
  `socket.connect(('127.0.0.1', p))` 遍历 `7897/11268/7890/10809/...`，或直接试直连。
- `-c http.proxy=` 只作用于**那一条命令**：`git ls-remote` / `git log origin/main`（不带 `-c`）会走全局死代理而报错，
  别据此判定远端有问题。
- `git ls-files` 默认 `core.quotepath=true`，非 ASCII 路径输出成 `\345\217\202...` 转义形式 →
  脚本遍历会误报「索引里有但工作区没有」。要加 `-c core.quotepath=false`。
- 查 CI 的 sha 参数必须是**完整 40 位**（API 返回完整 sha，传短 sha 会匹配不到 → 误判「还没有 run」）。
