# 2026-09-17

## 审计「没跑通」+ 修 EP5（与修前 EP4 同病）
- 用户问「还有哪里没跑通 优化下」。审计结论：HANDOFF 里两个历史卡点**早已闭环**——
  1. `agent/publisher.py::delete_video`（283 行）现已是有意「不自动删」（`code=340022` 人机验证，2026-09-05 用户决定放弃），非 stub；
  2. `publish_latest`（217 行）已修：用 `title_template` + 中文数字集数 + `validate_title` 门禁，标题带集数。
  真正没跑通的是 **EP5**：`outputs/ep5/ep5_film.mp4` 仍是 **11.50s**（6×56 帧，与修前 EP4 完全相同缺陷），无配音、未推 B站。
- **清理死代码**：删掉 `make_narration.py::_trim_silence`（全仓 0 调用，`stop_periods=1` 句内截断 bug，前次已加警示注释）。

## EP5 跑通（重渲 12 镜 + 配音，与 EP4 同规格）
- 分镜 `outputs/series_shots.json` 的 ep5 由 6 → **12 镜**（每句 2 镜：建立+细节，wide/close-up，保留 white clay-model 风格）；旧 6 镜备份 `series_shots.json.bak_ep5_6shot`。旧 6 镜产物备份 `outputs/_ep5_backup_6shot/`。
- 重渲：`run_series.py --ep 5 --frames 90 --free-every 4 --anchor auto --force --engine mmh3` →
  `outputs/ep5_series_film_mmh3.mp4` **39.50s / 25MB / 768×448@24fps / 12 镜 / 角色锚定(first) 生效**（task `GpBEsz`）。
- 配音：`make_narration.py --film outputs/ep5_series_film_mmh3.mp4 --out outputs/ep5_vo.mp4 --fit-film --fps 24 --series-script outputs/series_script.json --ep 5 --shots 12`
  → `outputs/ep5_vo.mp4` **39.50s / 22.18MB / 96kHz 立体声**，英文旁白(en-GB-SoniaNeural)+中英双语字幕+环境音(0.18)。
  6 段 tempo 全 ≤1.08（硬顶 1.15），各占 6.50s 槽位、无交叠（高阈值 RMS 验证：seg3/seg4 因 seg3 填满整槽而紧接，正常衔接）。
- 上传脚本 `_bili_upload_ep5.py` 已写好（复用 `_bili_upload_anime.py::upload_one`，标题《看见未来之前》第五集 · 雨城归途）。
- **已发布（用户「发布」）**：`python _bili_upload_ep5.py` → **bvid=`BV1LYeu63Edc`**，aid=117285708628906。
  回查校验（`view`，提交后 ~30s 转码完成）：title=《看见未来之前》第五集 · 雨城归途、duration 40s（实际 39.5s）、state=0 已发布、tid=172。
  bvid 存档 `outputs/_ep5_bvid.txt`。EP5 全链路闭环（扩镜→重渲→配音→发布）。

## 修「锚定资产」WebUI 加载失败（TypeError: Failed to fetch）
- 症状：pipeline 页「锚定资产 · 三视图 / 场景图」标签 `fetch('/api/anchor')` 报 `加载失败：TypeError: Failed to fetch`。
- 根因：**ai_movie_agent 的 Flask WebUI(:8000) 根本没在跑**（curl `http://127.0.0.1:8000/`=HTTP 000）。浏览器若开在 :3000 则看到的是**另一个项目**「梦中情家·装修沙盒」，非本 WebUI。非前端 bug。
- 修法：用主 venv 起后台常驻 `cli.py webui --host 127.0.0.1 --port 8000`（带 `NO_PROXY=127.0.0.1,localhost` 防内部 8188 探针走代理）。
- 验证：`/`=200(标题 AI Movie Agent)、`/api/anchor`=200(JSON, ready=true, 三视图/场景图均在, has_index=true)、`/api/anchor/file?name=mira_front.png`=200(真实 PNG 296500B)、`/api/anchor/index`=200。状态页 `outputs/webui_status.html`。
- 注：WebUI 属按需重启服务（约 15h 后被回收）；再报 Failed to fetch 重跑启动命令即可。已顺手把超 34KB 的 MEMORY.md 精简到 ~2.8KB。
