# 2026-09-16

## 决策：不做真人脸检测（用户明确否决，勿再提议）
- 起因：我在「实用性建议」里把「把真人脸检出率加进 preflight 做发布闸门」列成 #2；用户上一轮选 1/3/4/5 已跳过，
  本轮我追问是否补 → 用户回「**真人脸不要检测**」。
- **核查结论（先查再动，不凭印象）**：仓库里本来就**没有任何环节在跑人脸检测**——
  - `agent/qa.py` 的人脸指标是**可选能力**，`DEFAULTS["face_check"]=False`，只在 `qa.face_check` 显式为 true 且是人物镜时才走
    （且依赖 `insightface`，缺失自动降级跳过）。
  - 本机的 `config.yaml:147` 与 `config.example.yaml:168` **均为 `false`**。
  - `agent/preflight.py` **grep `face` 零命中** → 从未有人脸闸门。
  - `config.example.yaml` grep `face_swap|cartoonize|anime_redraw` 零命中 → `face_swap_ep.py` / `cartoonize.py` 是**独立脚本**，不在默认流水线。
  → 所以无需改代码逻辑，只需**把策略写死防止以后被翻开**。
- 落地（3 处文档，不改行为）：
  - `config.example.yaml`：`face_check` 注释改为首行即「⚠️ 项目策略：**不做真人脸检测**，保持 false 不要翻」，
    原来的「Haar 只认真人脸 / 动漫脸失效」降级为补充理由；并指向离线诊断脚本 `diag_face_rate.py`。
  - `README.md`：质检描述与配置表两处删掉「身份漂移 / 人脸」字样（原写「糊 / 静帧 / 全黑 / 可选身份漂移」），
    `qa.*` 行加注「本项目不做真人脸检测，`qa.face_check` 恒为 false」。
  - `MEMORY.md` 六、节加 🚫 条目：不做 preflight 人脸闸门、不在流水线跑人脸检出、`qa.face_check` 恒 false，
    并写明「该提议已被否决，别再提」；解决真人脸只走源头（G 阶段提示词锁动漫风格 + 动漫风格锚定图）。
- **教训**：我提 #2 时把它当成「用户硬性要求但无闸门」，但用户要的是**结果**（不出真人脸）而非**检测**。
  检测型闸门与用户的取向（反对打码/模糊、不接受检测开销与误判）冲突 → 应优先提**源头方案**（提示词 + 锚定图）。
