# 2026-09-20 工作日志

## cli.py call：真实长任务链路跑通 + 沙箱 worker 修复

- 真实演示 `call enhance_video`（ComfyUI 由前台 `supervise.py` 看护起，:8188=200）：
  `job_id f01dcbbb8e75` → `queued` → `running`(try1) → `done`(try2)，产物 `outputs/_demo_src_enhanced.mp4`(486KB)。
  完整 `job_id → get_job_status → done + result` 链路打通；`--wait` 进程内路径也验证可用（直接返回 handler 结果 JSON）。
- **关键 bug 修复**：`_spawn_detached` 加 `CREATE_BREAKAWAY_FROM_JOB`(0x01000000) 在本沙箱抛
  `[WinError 5] 拒绝访问`，worker 根本起不来。改为「先试带 flag、`except OSError` 回退 `DETACHED_PROCESS`」，
  启动失败也写进 job 状态（不再静默卡 queued）。commit `931ef69` 已推 main。
- **沙箱真相**：脱钩的后台 worker 在父命令结束时被沙箱整树回收 → `call <长任务>` 的 job_id
  **必须同一命令内**轮询（或 `--wait`）才能到 `done`；跨命令轮询永远卡 queued/running。
  README「单入口 JSON 调度器」段已补沙箱须知（doc-accuracy）。
- `supervise.py --once` 父进程退出 → ComfyUI 子进程被回收（8188 起不来）；起常驻服务用**前台** `supervise.py`。
- `pytest -q` → **477 passed**（无回归）。

## 收尾
- 清理 demo 产物（`_demo_src.mp4`/`_demo_src_enhanced.mp4`/`_demo_wait.mp4`/`.webmcp_jobs/` 均 gitignored，仅本地残留）。
- ComfyUI 后台任务（task cMWnkZ）仍运行，:8188=200；按需可停。
